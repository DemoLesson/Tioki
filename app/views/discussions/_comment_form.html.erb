<%= form_for @comment, :url => { :action => 'reply_to_discussion', :id => @discussion.id } do |f| %>
	<strong>Add Your Reply</strong><br />
	<%= f.text_area :body, :style => "width:650px" %><br />
	<%= f.submit "Add Reply" %>
<% end %>
