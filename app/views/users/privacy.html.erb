<section id="main-content" class="inner-content">
	<h1>Privacy Settings</h1>
	<p>Your Tioki connections have full access to your complete profile. However, you can control what the public sees on your profile by establishing your privacy settings below.</p>
	<p>All items with a check mark are visible to the public</p>

	<%= form_tag :action => "privacy" do %>
		<% APP_CONFIG['bitswitches']['user_privacy'].each do |bit, name| %>
			<fieldset>
				<input type="hidden" name="public[<%= name %>]" value="1" />
				<input type="checkbox" name="public[<%= name %>]" <%= raw "checked=\"checked\"" unless @user.privacy_public[name] %> value="0" />
				<%= name.gsub('_', ' ') %>
			</fieldset>
		<% end %>
		<strong>Resume Tab:</strong>  Who has access to the "resume" portion of your profile (i.e. Work experience, education, etc.)?
		<fieldset>
			<%= radio_button_tag 'resume', '1', true %> Everyone<br />
		</fieldset>
		<fieldset>
			<%= radio_button_tag 'resume', '0' %> Only organizations to which I have applied.<br />
		</fieldset>
		<fieldset>
			<%= submit_tag "Update Privacy", :class => "button huge-button" %>
		</fieldset>
	<% end %>
</section>
