$(function() {
	if(<%= @redirect ? 'true' : 'false' %>) window.location = '<%= @redirect %>';
	else 
		$('#<%= dom_id(@user) %>').removeAttr('href');
		$('#<%= dom_id(@user) %>').text('Requested');
});

