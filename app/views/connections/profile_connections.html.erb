<section id="main-content" class="inner-content">
	<span style="font-size:20px; font-family: Arial, Helvetica, 'Nimbus Sans L', sans-serif; color:#000; text-shadow:#ccc 1px 1px 2px;">
		<%= @user.name%>'s <%= ablang('connection.plural') %>
	</span><br />

	<div id="content">
	<% @connections.each do |connection| %>
		<br />
		<div class="item_jobs">
			<div class="item_picture">
				<% if connection.not_me(@user.id).avatar? %>
					<%= link_to image_tag(connection.not_me(@user.id).avatar.url(:thumb)), '/profile/' + connection.not_me(@user.id).slug %>
				<% else %>
					<%= link_to image_tag("dl_icons/no_avatar_medium.jpg"), '/profile/' + connection.not_me(@user.id).slug %>
				<% end %>
			</div>

			<div id ="connection_desc">
				<strong><%= link_to connection.not_me(@user.id).name, '/profile/' + connection.not_me(@user.id).slug %></strong><br />
				<% if !connection.not_me(@user.id).currentJob.nil? %>
					<%= connection.not_me(@user.id).currentJob.position %> at <%= connection.not_me(@user.id).currentJob.company %>
				<% end %><br />
				<%= connection.not_me(@user.id).location %><br /><br />
				<div>
					<% if connection.not_me(@user.id).id == self.current_user.id  %>
					<% elsif @my_connections.collect{|x| x.not_me.id}.include? connection.not_me(@user.id).id %>
						<%= link_to "Send Message", { :controller => 'messages', :action => 'new', :user_id_to => connection.not_me(@user.id).id.to_s }, {:class => "button bg-green"} %>
					<% else %>
						<%= link_to ablang('connection.connect'), { :controller => 'connections', :action => 'add_connection', :user_id => connection.not_me(@user.id).id }, :class => "button" %>
					<% end%>
				</div>
			</div>
		</div>
	<% end %>
	<%= will_paginate @connections %>
	</div>
</section>